var sum ( var a , var b )
{
    return a + b;
}

var ded ()
{
    var z = 1;

    return z;
}

var main ( )
{
    var x = sum ( 1 , 2 ) ;

    var ы = sum ( 1 , x ) ;

    x = ( 3 >= 4 ) && ( 2 ) ;

    out ( x ) ;

    in ( ы , x ) ;

    if ( x == 2 )
    {
        var t = 7;
        x = 6;
        out ( t ) ;
    }
    else if ( 0 )
    {
        out ( x ) ;
    }
    else
    {
        out ( ы ) ;
    }

    var ч = 5 * ( 7 - 4 ) + ы + ceil ( ы ) ;

    x = ( ч - ы + 7 + sum ( x , sum ( x , 1 ) ) ) / 5 ;

    out ( x ) ;

    x = ded ();

    return 2 - 4 ;
}


