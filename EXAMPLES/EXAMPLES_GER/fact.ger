var fac (var a)
{
    if ( a == 1 )
    {
        return 1 ;
    }

    var b = a - 1 ;

    return ( b + 1 ) * fac ( b ) ;
}


var main ( )
{
    var x = 0;

    input ( x ) ;

    x = fac ( x ) ;

    output ( x ) ;

    return 0 ;
}
