var sum ( var a , var b )
{
    return a + b;
}

var main ( )
{
    var ы = 5 * 6;
    var x = 0;

    input ( ы , x );

    var ч = 5 * ( 7 - 4 ) + ы + ceil ( ы );

    x = ( ч - ы + 7 ) / 5 ;

    output ( x );

    return 2 - 4 ;
}

var mul ( var a , var b )
{
    return a * b;
}


