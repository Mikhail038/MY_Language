var fac (var x, var y, var z)
{
    var a = 1;

    return (a + x - y) * z;
}

var main ()
{
    var x_z = 40;

    var z = 2;

    while (3 > 1)
    {
        fac (x_z - z, 1, z);
    }

    return 0;
}

